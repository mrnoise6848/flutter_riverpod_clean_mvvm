enum AuthenticationType { email, phoneNumber }

enum SnackBarType { info, success, error }

enum LocaleType { fa, en }

enum PrefKey {
  firstOpenApp,
  isLogin,
  phoneNumber,
  password,
  userToken,
  firstNameAndLastName,
  language,
}
